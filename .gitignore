# Xcode
build/
xcuserdata/
*.xcuserstate
DerivedData/
*.xcscmblueprint
*.xccheckout

# SPM
.build/
.swiftpm/
# Hosted CI restores SwiftPM checkouts here between runs.
.ci/

# Node (pairing plugin)
plugin/node_modules/

# Push Relay (Cloudflare Worker): local secrets and wrangler state.
# The APNs .p8 is a deploy-time secret and must never land in the repo.
relay/.dev.vars
relay/.wrangler/
relay/node_modules/
*.p8

# Python (test fixtures under scripts/fixtures)
__pycache__/
*.pyc

# macOS
.DS_Store

# Local-only notes and experiments
.local/

# testloop scratch artifacts; keep concise run reports as repository history
.testloop/*
!.testloop/reports/

# Agent workspace (research reports, scratch)
.claude/

# Android (android/): Gradle state, IDE files, and the local SDK path.
# `build/` above already covers every module's build output.
android/.gradle/
android/.kotlin/
android/local.properties
android/.idea/
android/*.iml
android/*/*.iml
